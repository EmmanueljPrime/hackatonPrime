{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-primary" onclick="this.disabled = true; this.form.submit();">
    {{ button_label|default('Save') }}
</button>
{{ form_end(form) }}